# geomINR runtime dependencies.
#
# NOTE: training uses the Muon optimizer from torch.optim, which requires
# torch >= 2.12 (you may need the PyTorch nightly / dev wheel index).
torch>=2.12
numpy>=1.24
SimpleITK>=2.3
omegaconf>=2.3
einops>=0.7
jaxtyping>=0.2
torchmetrics>=1.0
lpips>=0.1.4
tqdm>=4.65
wandb>=0.16            # optional logger; disabled by default (wandb.enabled=false)
